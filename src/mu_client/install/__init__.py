"""Installers that wire mu-client's capture spine into a real host's own config (Phase 0,
AGENT-INTEGRATION-AUDIT-AND-PLAN.md §4). ``claude_code`` is the only host wired this stage."""

from __future__ import annotations

__all__: list[str] = []
